{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "landing-page",
      "runtimeExecutable": "bun",
      "runtimeArgs": ["--smol", "-e", "const s=Bun.serve({port:3456,fetch(r){const u=new URL(r.url);const p=u.pathname==='/'?'/index.html':u.pathname;return new Response(Bun.file('docs/site'+p));}});console.log('http://localhost:3456')"],
      "port": 3456
    }
  ]
}
